import 'package:flutter/material.dart';

class SpeechOverlay extends StatelessWidget {
  const SpeechOverlay({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Scaffold(
        body: Text("wtf"),
      ),
    );
  }
}